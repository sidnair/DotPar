func main:void() {
    boolean x = true;
    if(x) {
        println(true);
    } else {
        println(false);
    }

    if(!x) {
        println(true);
    } elif(x) {
        println(false);
    } else {
        println(0);
    }

    if(!x) {
        println(true);
    } elif(x) {
        println(false);
    } elif(x) {
        println(true);
    } else {
        println(0);
    }

    if(!x) {
        println(true);
    } elif(!x) {
        println(false);
    }
}
