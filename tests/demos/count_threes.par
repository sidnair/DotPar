func main:void() {
  func fill_arr:number(number index) {
    return rand(10);
  }

  number[] arr = fill(fill_arr, 10000);
  number[] threes = [x for number x in arr if x == 3];

  number sol = len(threes);

  /*println(arr);*/
  println(sol);
}
