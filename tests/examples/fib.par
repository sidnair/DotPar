func fib:number(number n) {
    if (n == 0) {
        return 0;
    } elif (n == 1) {
       return 1;
    } else {
        return fib(n-1) + fib(n-2);
    } 
}

func main:void() {
    println(fib(10));
}